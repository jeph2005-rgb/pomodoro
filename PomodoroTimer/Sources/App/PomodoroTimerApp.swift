import SwiftUI

@main
struct PomodoroTimerApp: App {
    var body: some Scene {
        Window("Pomodoro", id: "main") {
            Text("Pomodoro")
                .frame(minWidth: 360, minHeight: 480)
        }
    }
}

// Server component. In Phase 3 this renders <PomodoroApp /> (a 'use client'
// component). For now it is a minimal placeholder so the scaffold is clean.
export default function Home() {
  return (
    <main>
      <h1>Pomodoro Timer</h1>
    </main>
  );
}

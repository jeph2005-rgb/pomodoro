// Server component. Renders the client-side PomodoroApp.
import PomodoroApp from '@/components/PomodoroApp';

export default function Home() {
  return (
    <main>
      <PomodoroApp />
    </main>
  );
}
